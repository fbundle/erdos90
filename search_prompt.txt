I am using an AI to write proof in this directory, here is what needed to finish the proof.
Could you help to do search and find the resources and write into assets/
assets/search_results/ should hold the details
assets/ should hold the original sources: PDFs, tex files, etc.

below is the instruction:

## Context

We're formalizing Theorem 1.1 from the OpenAI paper "Planar Point Sets with Many Unit Distances" (2026) in Lean 4 / Mathlib v4.30.0-rc2. The formalization has 2 remaining `sorry` gaps, both in `Erdos90/NumberFieldDeep_GSTower.lean`, function `gs_tower_levels_proved`.

**Current state after other agent's edits** (these are already done — do NOT redo):
- `classNumBound = Real.log(h_K)/f` in `CMTowerData`, with `hClassNum` proved tautologically via `Real.exp_log`. Good.
- The `h_classNumBound_zero` field was removed.
- In Assembly, `classNumBound_le_log_H` is now the single Minkowski bound sorry (was: `rw [h_classNumBound_zero]; exact hlog_H_nn` — now it's just `sorry` with a comment).

## What I need to close the gaps

### Gap S1: `h_div_conj_mem_Λ` — Q²-scaling valuation proof

The `CMTowerData` currently has:
```
splitPrimesFor : ∀ (t' : ℕ), SplitPrimeData K (t' * f)
h_div_conj_mem_Λ : ∀ (t' : ℕ) (ε₁ ε₂ : Fin (t' * f) → Bool) (α : K) (_ : α ≠ 0),
    FractionalIdeal.spanSingleton (𝓞 K)⁰ α * J_ideal K (splitPrimesFor t') ε₁ =
    J_ideal K (splitPrimesFor t') ε₂ →
    φ (NumberField.mixedEmbedding K (α / IsCMField.complexConj K α)) ∈ Λ
```

The valuation argument (already documented in `assets/search_results/valuation_formula_derivation.md`): for α with (α)J(ε₁) = J(ε₂), we have v_P(α/c(α)) ∈ {-2, 0, 2} at split primes. We need to show Q²·(α/c(α)) ∈ 𝓞_K where Q = product of rational primes below split primes, so that Φ(α/c(α)) ∈ Λ (since Λ = Φ(Q⁻²·𝓞_K) after lattice scaling).

**Questions I need answered:**

Q1. Find the EXACT signature and usage of `mem_integers_of_valuation_le_one` in `RingTheory/DedekindDomain/AdicValuation.lean`. Show me the full lemma statement. How do I convert `∀ v, v.valuation K x ≤ 1` into `x ∈ (algebraMap (𝓞 K) K).range`? What is `HeightOneSpectrum.valuation` — does it return a `ℤ` or `ℝ`?

Q2. Find the API for `Ideal.absNorm` in Mathlib. What lemmas exist for:
- `Ideal.absNorm_mul` (norm of product of ideals)
- `Ideal.absNorm_prime` (norm of a prime ideal = q^f where q is the rational prime below)
- The relationship between `Ideal.absNorm P` and the rational prime `q` such that `P ∩ ℤ = (q)`. Is there a lemma `Ideal.prime_above` or `Ideal.primeBelow` that gives this rational prime?

Q3. How do I get from `Ideal.absNorm P = q` to `P ∣ (q)` and `(q) = P * conjIdeal K P` for split primes in a CM field? Are there Mathlib lemmas about splitting of rational primes in Galois extensions?

Q4. Find the exact API for `FractionalIdeal.count` in `RingTheory/DedekindDomain/Factorization.lean`:
- `count_mul` (already known)
- `count_spanSingleton` — does it exist? What's the formula for `count` of a principal fractional ideal?
- `count_self` — what does it return for `count K v (v.asIdeal : FractionalIdeal R⁰ K)`?
- Is there a lemma connecting `FractionalIdeal.count K v I` to the additive valuation ord_v(I)?

### Gap S2: Minkowski class-number bound in Assembly

The other agent changed `classNumBound` to `Real.log(h_K)/f` (tautological `hClassNum`). Now `classNumBound_le_log_H : Real.log(h_K)/f ≤ log_H` is the real gap.

**Questions I need answered:**

Q5. Find `exists_ideal_in_class_of_norm_le` in `NumberTheory/NumberField/ClassNumber.lean`. Show me the FULL theorem statement. Does it bound the number of ideal classes, or just give an ideal representative per class? What is the type of `M K` (the Minkowski bound)?

Q6. Find the exact API for `IsCyclotomicExtension.discr_odd_prime` in `NumberTheory/Cyclotomic/Discriminant.lean`. What fields/lemmas are available for the absolute discriminant of ℚ(ζ_p)? Is there `discr_prime_pow` for p^1 (just p)?

Q7. Is there any Mathlib lemma that gives `log(h_K)/f ≤ C * log(rd(K))` for any constant C? Search for "Brauer-Siegel", "class number bound", or "Minkowski bound" across all of Mathlib.

Q8. For cyclotomic fields specifically: is there `NumberField.discr_cyclotomic` or similar giving |disc(ℚ(ζ_p))| = p^{p-2}? What's the exact file path?

### Gap S3 (structural): Lattice scaling by Q²

Q9. In `gs_tower_levels_proved`, the lattice Λ is currently:
```
let basis : Module.Basis ... ℝ (Fin f → ℂ) := (mixedEmbedding.latticeBasis K).map φ
let Λ : AddSubgroup (Fin f → ℂ) := (Submodule.span ℤ (Set.range basis)).toAddSubgroup
```
This gives Λ = Φ(𝓞_K). I need Λ_Q = Φ(Q⁻²·𝓞_K). Find:
- Can I use `basis.map` with a scalar multiplication `LinearEquiv` on `(Fin f → ℂ)`?
- Or should I pre-compose φ with scaling on `mixedSpace K`? What's the API for `smul` on `mixedSpace K`?
- Is there a lemma that `q⁻² • (ZSpan.fundamentalDomain basis)` is a fundamental domain for the scaled lattice?

Q10. For the separation bound `‖v i‖ ≥ D₀⁻¹` with the scaled lattice: currently D₀=1 and the bound uses the product formula. For Λ_Q = Φ(Q⁻²·𝓞_K), the separation becomes `‖v i‖ ≥ Q⁻²`. How should I thread this through? The current return type uses `base.D₀⁻¹`. Options: (a) add a level-specific D₀' to the return type, (b) change base.D₀ to Q². Which approach is cleaner given that `base` is shared across all tower levels?
